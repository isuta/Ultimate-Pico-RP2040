# データ駆動型 統合制御システム (NeoPixel/OLED/DFPlayer Mini)

このプロジェクトは、MicroPython環境（ESP32/ESP8266/RP2040など）で動作する、データ駆動型の統合制御システムです。LED、OLEDディスプレイ、およびオーディオ再生（DFPlayer Mini）の制御を、`scenarios.json` ファイルに基づいて実行します。

## � 変更履歴（最近の作業）

2025-11-01: main.py のスリム化と表示・初期化ロジックの分割を行いました。

- 変更点の概要:
  - `volume_control.py` を導入し、ポテンショ（ADC）→ボリューム制御ロジックを `main.py` から分離しました。これによりボリューム関連の読み取り・デッドゾーン処理・DFPlayer への反映をモジュール化しています。
  - `hardware_init.py` を追加して、OLED / NeoPixel / DFPlayer / ボタン / ADC / 内蔵LED の初期化処理を集約しました。初期化失敗時のフォールバック処理もここにまとめています。
  - `display_manager.py` を追加し、OLED 表示の呼び出しを一元化しました。`main.py` は直接 `oled_patterns` を呼ばず、`DisplayManager` を通して表示を行います。

- 目的と利点:
  - `main.py` をオーケストレータに近い薄い役割にし、個別コンポーネントのテストとメンテナンスを容易にしました。
  - ハードウェア依存コードを分離することで、実機デバッグやユニットテストの導入がしやすくなりました。

- デプロイ手順（簡易）:
  1. `main.py`, `hardware_init.py`, `display_manager.py`, `volume_control.py` をデバイスにアップロードします（mpremote または Thonny を推奨）。
  2. デバイスをリセットしてシリアルログで初期化メッセージと OLED 表示を確認してください。
  3. ポテンショやボタンでボリューム／再生の挙動を検証します。

もし README の内容を別ブランチ向けの CHANGELOG 形式でまとめたい場合は、その形式でも追記します。

## �🚀 システムの概要

このシステムは、ハードウェアの配線や設定をすべて `config.py` に分離しており、新しいハードウェア構成への変更が容易です。

**🛡️ ハードウェア堅牢性**: 各ハードウェアモジュール（DFPlayer、OLED、NeoPixel、ボタン）は独立して初期化され、一部が接続されていなくても他の機能を利用できます。最悪の場合、何も接続されていなくても内蔵LEDとコンソール出力でシステムの動作確認が可能です。

| モジュール | 役割 |
| ----- | ----- |
| **NeoPixel** | 抽選結果やアニメーションの光演出 |
| **OLED (SSD1306)** | ステータス、選択中のシナリオ、メッセージの表示 |
| **DFPlayer Mini** | 効果音やBGMの再生 |
| **タクトスイッチ** | 抽選開始、モード切替、シナリオ選択 |
| **内蔵LED** | システム状態とシナリオ実行状況の視覚的表示 |
| **ポテンショメータ** | リアルタイムボリューム調整 |

---

## ⚙️ セットアップと設定

### 1. 必要なファイル

以下のファイルをMicroPythonデバイスにアップロードしてください。

**プロジェクトファイル (デバイスのルートディレクトリに配置):**

* `main.py`
* `config.py`
* `effects.py`
* `led_patterns.py`
* `oled_patterns.py`
* `sound_patterns.py`
* `onboard_led.py` *(新規追加)*
* `scenarios.json`

**外部ライブラリ (Thonnyなどで`lib`フォルダに配置):**

* `ssd1306.py` (SSD1306 OLEDドライバ)
* `neopixel.py` (NeoPixel制御ライブラリ)

### 2. ハードウェア設定 (`config.py`)

`config.py` ファイルを開き、ご自身の配線に合わせて以下の値を**正確に**設定してください。

| 設定項目 | 説明 | 変更範囲の例 |
| ----- | ----- | ----- |
| `OLED_SDA_PIN`, `OLED_SCL_PIN` | OLED接続先のGPIOピン番号。 | `16`, `17` など |
| `OLED_WIDTH`, `OLED_HEIGHT` | 使用するOLEDのピクセル数。 | `128`, `64` |
| `DFPLAYER_TX_PIN`, `DFPLAYER_RX_PIN` | DFPlayer Mini接続先のUARTピン。 | `12`, `13` など |
| `DFPLAYER_BUSY_PIN` | DFPlayerのBUSY信号が接続されたGPIOピン。 | `14` など |
| `DFPLAYER_DEFAULT_VOLUME` | DFPlayerの初期音量。（0:ミュート 〜 30:最大） | `15` (推奨) |
| `BUTTON_PIN` | タクトスイッチが接続されたGPIOピン。 | `18` など |
| `ONBOARD_LED_PIN` | 内蔵LED指定。（通常は`"LED"`で固定） | `"LED"` |
| `POTENTIOMETER_PIN` | ボリューム用ポテンショメータのADCピン。 | `26` など |
| `NEOPIXEL_STRIPS` | 各NeoPixelストリップのピン番号とLED数。 | `'LV1': {'pin': 20, 'count': 6}` |

### 3. シナリオのカスタマイズ (`scenarios.json`)

すべての光と音の演出は `scenarios.json` にて定義されます。このファイルを編集することで、**Pythonコードを一切触らずに**新しい演出を追加できます。

#### シナリオキーのルール

* **`"1"`から始まる任意の整数（文字列）**で定義されたキー（例: `"1"`, `"2"`, `"36"`）が、ランダム再生の**抽選対象**となります。

* **数字以外のキー**（例: `"_101"`, `"_README_"`）は、システムやデバッグ用の予約キーと見なされ、演出再生の**抽選対象外**となります。

#### 利用可能なコマンドフォーマット

| コマンドタイプ | フォーマット | 説明 |
| ----- | ----- | ----- |
| **`sound`** | `["sound", folder_num (int), file_num (int)]` | DFPlayer Miniの指定フォルダ/ファイル番号の音声を再生します。 |
| **`delay`** | `["delay", duration_ms (int)]` | 演出の実行を一時停止します（ミリ秒単位）。停止フラグで中断可能です。 |
| **`led` (新形式)** | `{"type": "led", "command": "fill", "strip": "all"/"LV1"/etc, "color": [R, G, B], "duration": ms}` | 指定ストリップを指定色で塗りつぶし、duration後に維持（0なら瞬時設定）。 |
| **`led` (新形式)** | `{"type": "led", "command": "off"}` | 全LEDを瞬時に消灯します。 |
| **`effect`** | `["effect", "off"]` | すべてのNeoPixel LEDを瞬時に消灯します。 |
| **`effect`** | `["effect", "global_set", [indices (list)] or strip_name (str), R (int), G (int), B (int)]` | 指定された**グローバルLEDインデックス**のリスト、**ストリップ名**（例: `"LV1"`）、または**`"all"`**に色を瞬時に設定します。 |
| **`effect`** | `["effect", "fade", [indices (list)] or strip_name (str), [start_R, start_G, start_B], [end_R, end_G, end_B], duration_ms (int)]` | 指定された**インデックス**、**ストリップ名**、または**`"all"`**を、指定時間かけて開始色から終了色へ滑らかにフェードさせます。 |
| **`effect` (旧式)** | `["effect", "local_set (旧式)", strip_name (str), led_index (int/"ALL"), R, G, B, duration_ms (int)]` | 従来のストリップ単位の設定。**非推奨**。duration\_msだけ点灯後に元に戻ります。 |

---

## 🕹️ 基本的な使い方

システム起動後、タクトスイッチの操作によって2つのモードに切り替わります。

**💡 内蔵LED動作状況表示**:
- **起動時**: 短時間点滅（初期化完了）
- **シナリオ実行中**: 点灯
- **シナリオ完了/待機中**: 消灯
- **強制停止時**: 即座に消灯

**🛡️ ハードウェア故障時の動作**:
各ハードウェアが接続されていない場合でも、利用可能な機能のみでシステムが動作します。ボタンが利用できない場合は、**起動約1分後から1分間隔でランダムシナリオが自動実行**されます。

### 1. 通常モード (初期状態)

OLEDに「Push the button」が表示されます。

  * **短押し**: ランダムなシナリオ（`scenarios.json`からランダムな番号）が選ばれ、演出が開始されます。

  * **アイドル自動再生**: 5分間操作がない場合、その後1分間隔でランダムシナリオが自動実行されます。### 2. セレクトモード (デバッグ/選択)

電源投入直後、**1秒以上ボタンを長押し**したまま起動すると、セレクトモードに入ります。OLEDに「Select Mode」と表示されます。

  * **短押し 1回**: 選択中のシナリオ番号が**次に進みます** (`1` → `2` → `3`...)。

  * **短押し 2回 (連続)**: 選択中のシナリオ番号が**前に戻ります** (`3` → `2` → `1`...)。

  * **長押し (1秒以上)**: 画面に表示されている**選択中のシナリオ**の演出を開始します。

  * **再生中の短押し**: 実行中の演出を強制的に停止させます。

---

## 🔧 トラブルシューティング

### ハードウェア初期化エラー

各ハードウェアモジュールは独立して初期化されるため、一部が接続されていなくても他の機能を利用できます：

| 症状 | 対処法 |
| ----- | ----- |
| **ボタンが効かない** | コンソール専用モードに自動切り替え。約1分後から自動再生開始。 |
| **OLEDに何も表示されない** | コンソール出力で状況確認。機能は正常動作。 |
| **LEDが光らない** | 利用可能なストリップのみで動作。内蔵LEDで状況確認。 |
| **音が出ない** | LED演出は正常動作。DFPlayer接続を確認。 |
| **何も接続されていない** | 内蔵LEDとコンソール出力で動作確認可能。 |

### 起動時のシステム状態確認

起動時にコンソールに以下が表示されます：
```
=== System Ready ===
Button: Available / Console Mode
OLED: Available / Console Output  
Audio: Available / Disabled
LED: Available / Disabled
Onboard LED: Available / Disabled
Volume Control: Available / Disabled
===================
```

### コンソール専用モードの動作

ボタンが利用できない場合の自動動作：
- **初回開始**: 起動約1分後（アイドルタイムアウトをスキップ）
- **自動再生間隔**: 1分（60秒）
- **動作確認**: 内蔵LED点灯/消灯とコンソール出力

#### タイムライン例（全ハードウェア未接続時）
```
00:00 - システム起動（コンソール専用モード）
01:00 - 最初の自動ランダム再生実行
02:00 - 2回目の自動ランダム再生実行
03:00 - 3回目の自動ランダム再生実行
... (1分間隔で継続)
```
